// Edit this file with your Supabase project's public URL and anon key.
// These are SAFE to expose in the browser (anon key is public by design;
// Row Level Security in supabase/schema.sql is what actually protects data).
//
// In production we recommend setting these as Vercel env vars (SUPABASE_URL,
// SUPABASE_ANON_KEY) and substituting them at build time. For this prototype
// we just hand-edit the file.

window.GSS_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "REPLACE_WITH_YOUR_ANON_KEY",
};
